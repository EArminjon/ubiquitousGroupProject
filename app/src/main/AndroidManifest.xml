<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.example.enguerrand_robin_benjamin">

    <application
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".CreateQuizzActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".HomeAdminActivity" />
        <activity
                android:name=".RegisterActivity"
                android:parentActivityName=".EnguerrandRobinBenjamin" />
        <activity
                android:name=".LoginActivity"
                android:parentActivityName=".EnguerrandRobinBenjamin" />
        <activity android:name=".EnguerrandRobinBenjamin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>